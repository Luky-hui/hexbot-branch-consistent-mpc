# OCS2 Hexbot Seed

这个目录保存的是 `hexbot_locomotion_mpc` 包自己使用的 Hexbot 六足 seed 配置副本。

目的：

- 给桥接层、导出脚本和后续接入代码一个稳定的本地参考。
- 即使上游 `ocs2_hexbot_legged_robot` 还在继续改，这里也保留一份不跟着漂移的恢复基线。

当前包含：

- `hexbot_mapping.yaml`
- `reference.info`
